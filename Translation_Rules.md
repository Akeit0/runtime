## 1. ライセンスと帰属
- 本リポジトリの翻訳物は **MIT License** に従います。
- 各翻訳ファイルの先頭に次の **メタデータヘッダ（HTMLコメント）**を付与します:

<!--
Translated from: docs/<path>.md
Source SHA: cf2786c01ce
Translator: <name>
Updated: YYYY-MM-DD
notes: 
License: MIT
-->

## 2. 進め方
対応関係
翻訳元　docs/
翻訳先 docs_ja/
TRANSLATION_PROGRESS.mdで管理
## 3. 翻訳の基本原則（優先度順）

1. **正確さ最優先**：原文の意味・意図・強調を保持。誤解を招く意訳や脚色は禁止。
2. **用語一貫性**：後述の用語ポリシーに厳密に従い、文書全体で統一。
3. **可読性の確保**：日本語として自然に。ただし不要な説明追加は避ける。
4. **構造保持**：見出し階層・表・箇条書き・相対リンク・画像パスを維持。

---

## 4. 文体・表記ルール

* 文体は \*\*常体（〜である）\*\*で統一。
* 句読点は **「、。」**。技術用語・略語・単位は **半角**。
* 略語（JIT, IL, AOT など）は **半角大文字**で表記。
* UI 文字列やボタン名は **原文のまま**（必要に応じて訳注で補足）。

---

## 5. 用語ポリシー（専門用語は訳さない）
**原則**：専門用語・識別子は **英語のまま**本文に残す。必要に応じて **末尾の「訳注」セクション**に日本語説明をまとめる（本文に括弧注を多用しない）。

- **訳さない対象（例）**
  - API 名・型名・メソッド・プロパティ・イベント・例外名・エラーコード  
    例: `Span<T>`, `HttpClient`, `GC.Collect`, `Task<TResult>`
  - 名前空間・アセンブリ名・NuGet パッケージ名
  - 言語キーワード・CLI 名・サブコマンド・フラグ  
    例: `async`, `await`, `unsafe`, `dotnet build`, `--configuration`
  - ランタイム固有用語：CLR, IL, JIT, AOT, BCL, FCL, GC, LOH, P/Invoke, Tiered Compilation, Interop など
  - OS/ディストリ名・ファイル/ディレクトリ名・環境変数・レジストリキー

- **訳語の扱い**
  - 一般概念語（例：allocation, pinning, managed/unmanaged など）は本文では英語のまま。必要なら **訳注**に「割り当て／固定（ピン留め）／管理対象・非管理」などの説明を記載。

---

## 6. 訳注（注釈）
- 各ファイル末尾に **「# 訳注」**セクションを作り、**番号付きリスト**で記載する。  
- 本文側は必要最小限のみ参照する（例：`…とする[^1]` または `(注1)`）。  
- 訳注には **背景・日本語慣用訳・参考リンク（任意）**を簡潔に書く。

**本文側の例**
The GC uses generations to optimize collection[^1].

**末尾セクション例**
# 訳注
1. generations: .NET の GC における世代分けの概念。短命オブジェクトと長命オブジェクトを分離し、収集を最適化する。

---

## 7. コード・マークアップ
- **コードブロック／インラインコードは翻訳しない**（コメントを除く）。  
- サンプル出力・エラーメッセージ・プレースホルダ（`<T>`, `{name}`, `%PATH%` 等）は **改変しない**。  
- YAML/JSON/XML などの構造は **壊さない**。キー名は変更しない。  
- Markdown の見出しレベル、強調、引用、テーブル、脚注、アンカーは原文に準拠。  

---

## 8. リンク・画像
- **相対リンクは原構造を維持**。訳先が `docs_ja/` に存在する場合のみ相互リンクを `docs_ja/...` へ差し替える。未訳は `docs/...` を参照。  
- 画像は原文のパスを再利用。**alt テキストは日本語化可**だが、ファイル名・図番号は変更しない。  
- 画像・外部資料のクレジットやライセンス表記は **必ず保持**する。  

---

## 9. ファイル構成
- `docs_ja/` は **`docs/` と同じディレクトリ構造をミラー**する。  
  例：`docs/architecture/garbage-collector.md` → `docs_ja/architecture/garbage-collector.md`  
- **ファイル名は原文と同一**のまま（拡張子含む）。  

---

## 10. 禁止事項
- 原文にない情報の追加・推測・脚色。  
- 注意喚起や仕様の強度を弱める言い換え。  
- ガイド外の独自訳（専門用語の恣意的な日本語化）。  
- ライセンス・クレジットの削除や改変。  

---

## 11. 最終チェックリスト
- [ ] 原文の意味・意図を正しく保持している  
- [ ] 専門用語・識別子を **英語のまま**にしている  
- [ ] 訳注は末尾に集約し、本文から最小限の参照のみ  
- [ ] コード／設定／プレースホルダを改変していない  
- [ ] 見出し・表・箇条書き・リンク・画像パスが崩れていない  
- [ ] メタデータコメント（Source commit / License 等）を付与済み 

---



# <原文タイトルの日本語訳>

<本文（専門用語は英語のまま。必要なら脚注や訳注を参照）>

# 訳注
1. <term>: <日本語説明>